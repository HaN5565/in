import React from 'react';
import s from '../Team.module.css'

const TeamTitle = () => {
    return (
        <h1 className={s.teamTitle}>
            Наша команда
        </h1>
    );
};

export default TeamTitle;